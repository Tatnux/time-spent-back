query ($group: ID!, $first: Int) {
    group(fullPath: $group) {
        iterations(sort: CADENCE_AND_DUE_DATE_DESC, state: closed, first: $first) {
            nodes {
                id
                dueDate
                startDate
                state
                iterationCadence {
                    title
                }
            }
        }
    }
}