query ($group: ID!) {
    group(fullPath: $group) {
        name
        groupMembers {
            nodes {
                user {
                    id
                    name
                    username
                    publicEmail
                    avatarUrl
                    lastActivityOn
                }
            }
        }
    }
}